<h1>Organizations</h1>

<ul>
    <% @organizations.each do |organization| %>
        <li>
            <a href="/organizations/<%= organization.id %>">
                <%= organization.name %>
            </a>
        </li>    
    <% end %>
</ul>
